class Solution {
    public int removeDuplicates(int[] nums) {
        int n=nums.length;
        int[] arr=new int[];
        for(int i=o;i<n;i++){
            for(int j=1;j<n;j++){
                if(nums[i]!=nums[j]){
                    arr[]
                }

            }

        }
        
    }
}
